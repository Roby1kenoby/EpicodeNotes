Se uso una arrow function su una sola riga, il return è "automatico" (perchè c'è solo una cosa che sto restituendo).

Se invece devo fare più istruzioni (quindi tra graffe) allora devo inserire io un return.