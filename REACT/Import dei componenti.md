Quando faccio l'import di un componente, se nel file di export c'è un DEFAULT, allora devo metterlo senza graffe.
Per tutti gli altri elementi invece che non hanno il default, devo metterli tra graffe.